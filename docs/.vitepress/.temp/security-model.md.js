import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Security Model","description":"","frontmatter":{},"headers":[],"relativePath":"security-model.md","filePath":"security-model.md","lastUpdated":1780041277000}');
const _sfc_main = { name: "security-model.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="security-model" tabindex="-1">Security Model <a class="header-anchor" href="#security-model" aria-label="Permalink to &quot;Security Model&quot;">​</a></h1><p>Neutrx is designed for Node.js 18+ backend services making outbound HTTP calls. Its main security goal is to make dangerous outbound targets and credential leaks harder to trigger by accident.</p><h2 id="profiles" tabindex="-1">Profiles <a class="header-anchor" href="#profiles" aria-label="Permalink to &quot;Profiles&quot;">​</a></h2><ul><li><code>strict</code>: use for user-controlled URLs, webhook targets, admin tools, and high-risk egress.</li><li><code>standard</code>: default production service-to-service profile.</li><li><code>legacy</code>: trusted migrations and local testing only.</li></ul><p>Deprecated profile aliases are normalized internally for migration compatibility. New code should use only <code>strict</code>, <code>standard</code>, or <code>legacy</code>.</p><h2 id="ssrf-controls" tabindex="-1">SSRF Controls <a class="header-anchor" href="#ssrf-controls" aria-label="Permalink to &quot;SSRF Controls&quot;">​</a></h2><p><code>strict</code> and <code>standard</code> block:</p><ul><li><code>localhost</code>, loopback, <code>0.0.0.0</code>, private IPv4, carrier-grade NAT, link-local, and metadata IP ranges</li><li>IPv6 loopback, unique-local, link-local, metadata, and IPv4-mapped IPv6 forms</li><li>decimal, octal, and hexadecimal IPv4 variants where Node URL parsing allows them</li><li>cloud metadata hosts such as <code>metadata.google.internal</code></li><li>denied hosts and hosts missing from an allow-list</li></ul><p>The Node HTTP adapter validates DNS answers and pins the validated lookup for the request.</p><h2 id="unix-sockets" tabindex="-1">Unix Sockets <a class="header-anchor" href="#unix-sockets" aria-label="Permalink to &quot;Unix Sockets&quot;">​</a></h2><p><code>socketPath</code> is a local transport escape hatch for HTTP-over-Unix-socket services such as Docker Engine. When <code>socketPath</code> is set, the adapter connects to the absolute local socket path and treats the URL host as the HTTP <code>Host</code> header only. DNS, SSRF, private-IP, HTTPS, and egress-policy network checks are skipped for that synthetic host because there is no outbound TCP connection.</p><p>Neutrx still rejects relative socket paths, null bytes, CR/LF characters, proxy use with sockets, HTTPS socket URLs, unsafe headers, and URL credentials outside <code>legacy</code>. Treat <code>socketPath</code> as privileged configuration and do not accept it from untrusted input.</p><h2 id="redirect-safety" tabindex="-1">Redirect Safety <a class="header-anchor" href="#redirect-safety" aria-label="Permalink to &quot;Redirect Safety&quot;">​</a></h2><p>Each Node HTTP redirect target is revalidated. Cross-origin or downgrade redirects strip:</p><ul><li><code>Authorization</code></li><li><code>Cookie</code></li><li><code>Proxy-Authorization</code></li><li>sensitive custom headers such as API keys, tokens, passwords, and client secrets</li></ul><p>When a redirect changes a body method to <code>GET</code>, body headers are removed.</p><h2 id="secret-redaction" tabindex="-1">Secret Redaction <a class="header-anchor" href="#secret-redaction" aria-label="Permalink to &quot;Secret Redaction&quot;">​</a></h2><p>Use <code>error.toJSON()</code> and <code>getMetrics()</code> output for logs. Do not log raw request configs, raw headers, or raw response objects.</p><p>Redaction covers common secret names in URLs, headers, context, and response data. OpenTelemetry attributes avoid query strings by default.</p><h2 id="known-limits" tabindex="-1">Known Limits <a class="header-anchor" href="#known-limits" aria-label="Permalink to &quot;Known Limits&quot;">​</a></h2><ul><li>Browser builds cannot provide Node DNS validation.</li><li>Custom adapters must preserve Neutrx security semantics if they follow redirects internally.</li><li>Disabling SSRF checks or using <code>legacy</code> for untrusted URLs is unsafe.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("security-model.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const securityModel = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  securityModel as default
};
